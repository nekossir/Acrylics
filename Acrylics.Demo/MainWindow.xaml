<Window x:Class="Acrylics.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ac="clr-namespace:Acrylics;assembly=Acrylics"
        xmlns:local="clr-namespace:Acrylics.Demo"
        mc:Ignorable="d" Background="#222" Foreground="#FFF" x:Name="window"
        Title="Acrylics demo" Height="320" Width="480" FontFamily="Arial">
    <WindowChrome.WindowChrome>
        <WindowChrome
            UseAeroCaptionButtons="False"
            CornerRadius="0"
            CaptionHeight="0"
            GlassFrameThickness="1"
            ResizeBorderThickness="4"
            />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="EmptyTabControl" TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ChromeButton" TargetType="{x:Type Button}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Border x:Name="border" Background="#0000" BorderThickness="0" SnapsToDevicePixels="true">
                            </Border>
                            <Border x:Name="border1" Background="{TemplateBinding Foreground}" Opacity="0"/>
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Opacity" TargetName="border1" Value="0.1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Opacity" TargetName="border1" Value="0.2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid x:Name="grid">
            <TabControl x:Name="carousel" Style="{StaticResource EmptyTabControl}" SelectedIndex="{Binding SelectedIndex, ElementName=selector}">
                <TabItem Header="Ocean waves">
                    <Image Stretch="UniformToFill" Source="/Assets/ocean.jpg"/>
                </TabItem>
                <TabItem Header="Cherry blossom">
                    <Image Stretch="UniformToFill" Source="/Assets/cherry.jpg"/>
                </TabItem>
                <TabItem Header="Sunrise plains">
                    <Image Stretch="UniformToFill" Source="/Assets/plains.jpg"/>
                </TabItem>
                <TabItem Header="Pine forest">
                    <Image Stretch="UniformToFill" Source="/Assets/forest.jpg"/>
                </TabItem>
            </TabControl>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10, 0" HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBlock Text="Acrylics" FontWeight="Bold"/>
            <TextBlock Text=" Demo app" Opacity="0.5"/>
        </StackPanel>
        <ComboBox x:Name="selector" Width="100" Grid.Row="1" Margin="10,5" HorizontalAlignment="Right" ItemsSource="{Binding Items, ElementName=carousel}" SelectedIndex="0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Header}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Border Background="{ac:AcrylicBrush grid}" Margin="50">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" TextAlignment="Center">
                <Run Text="Example content"/>
                <LineBreak/>
                <LineBreak/>
                <Run Text="Border with AcrylicBrush" FontSize="16" Foreground="#4FFF"/>
            </TextBlock>
        </Border>
        <Grid Background="#0000" Height="30" VerticalAlignment="Top" MouseDown="Caption_MouseDown"/>
        <Button Click="exit_Click" Style="{StaticResource ChromeButton}" Height="30" Width="46" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Grid>
                <Border Width="13" Height="1" Background="{Binding Foreground, ElementName=window}" RenderTransformOrigin="0.5,0.5">
                    <Border.RenderTransform>
                        <RotateTransform Angle="45"/>
                    </Border.RenderTransform>
                </Border>
                <Border Width="13" Height="1" Background="{Binding Foreground, ElementName=window}" RenderTransformOrigin="0.5,0.5">
                    <Border.RenderTransform>
                        <RotateTransform Angle="-45"/>
                    </Border.RenderTransform>
                </Border>
            </Grid>
        </Button>
    </Grid>
</Window>
